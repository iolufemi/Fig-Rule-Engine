'use strict';
import {should} from 'chai';
should();

import app from '../app.js';

describe('#Run rule engine', function(){
    it('should load the rules');
    it('should load the facts');
    it('should return the result');
    it('should fail the rules');
    it('should pass the rules');
    it('should run multiple rules');
    it('should try another rule when one fails');
    it('should convert rule to JSON');
});
